import "./ContButton.css";

const ContButton = () => {
  return (
    <div className="button-container">
      <button className="cont-button"> {">"} </button>
    </div>
  );
};

export default ContButton;
